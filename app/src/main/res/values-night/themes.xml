<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuizApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/real_purple</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/real_purple</item>
        <item name="colorSecondaryVariant">@color/real_purple</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="QuizToolbar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="background">@color/black</item>
        <item name="titleTextColor">@color/white</item>
    </style>
    <style name="VariantStyleButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_marginHorizontal">51dp</item>
        <item name="backgroundTint">@color/grey</item>
        <item name="android:radius">32dp</item>
        <item name="android:textColor">@color/real_purple</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/real_purple</item>
    </style>

</resources>